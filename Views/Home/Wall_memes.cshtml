@model IEnumerable<Im.Models.Memes>

<div class="Wall_memes_all_memes_wall">
    @{
//создание постов и сама стена
        <ul>
                        @if (Model.Count() == 0)
            {
                            <li>
                                <div class="div_color_white">
                                    <p>Нет записей</p>
                                </div>
                            </li>
                                }
                                @foreach (var i in Model)
                                {
<li>
                            <div class="div_color_white">
                                <p>@i.Description</p>
                                @if (i.Image != null)
                                {
                                    Html.Raw(string.Concat("<img  class=''  id =''", " src=\"data:image/jpeg;base64,"
                    , Convert.ToBase64String(i.Image), "\" />"));
                                }
                                @if (i.Images != null)
                                {
                                    foreach (var i2 in i.Images)
                                    {
                                        Html.Raw(string.Concat("<img  class=''  id =''", " src=\"data:image/jpeg;base64,"
                   , Convert.ToBase64String(i.Image), "\" />"));
                                    }
                                }
                                 <hr />
                                <div>
                                    <div class="Memes_under_block_small">Лайков: @(i.Liked_id.Count()-1)</div>
                                    <div class="Memes_under_block_small">Репостов:@(i.Repost_id.Count() - 1)</div>
                                    <div class="Memes_under_block_small">Репостнуть</div>
                                </div>
                            </div>
                        </li>
                    }
</ul>
    }
</div>
